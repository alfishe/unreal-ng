# Dependencies for tools/workload-monitor/process-workload.py
#
# NOTE: On Linux, process-workload.py requires ZERO external dependencies
# and runs with standard Python 3 (reading directly from /proc).
#
# On macOS and Windows, psutil is recommended for full process and thread metrics:
psutil>=5.9.0
