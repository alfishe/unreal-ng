# DONE — Audio sync: multirate core (2026-08-17)

**Status:** complete.

## What landed
- Multirate core plan (phases through 6): `[SOUND] CoreRate` configuration, band-limited
  synthesis resamplers per source, DRC resampler + rate control between core and device
  rates (±0.5% trim on ring-buffer occupancy), device-native output rate, recording tap
  upstream of resampling. FIR tables generated by `generate_fir_tables.py`.

## Evidence
- `core/src/emulator/sound/soundmanager.{h,cpp}` ("multirate plan phase 6" comments,
  `ResamplerDRC`, per-chip resampler restart on rate change),
  `core/src/common/sound/filters/resampler_drc.h`, `core/src/emulator/platform.h:517+`.

## Follow-ups
- None.
