<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Allow JIT compilation and dynamic code generation (needed by Z80 emulator core) -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    
    <!-- Allow loading unsigned executable memory (needed for emulator dynamic code) -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    
    <!-- Allow dylibs signed by any team ID (needed for Qt frameworks in app bundle) -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    
    <!-- Allow dyld environment variables (useful for debugging Qt plugins) -->
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
</dict>
</plist>
